pipeline {
    agent any

    stages {
        stage ('Print') {
            steps {
                echo "Hello Devops Engineers"
                sh 'docker build -t lijitijil/index:html .'
                            }
        }
    }
}
